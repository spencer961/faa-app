import Placeholder from '../components/Placeholder.jsx'

export default function Dashboard() {
  return <Placeholder title="Dashboard" note="Your command center is migrating next — client cards, links & files, and the new at-a-glance summaries." />
}
